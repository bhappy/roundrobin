/* --- ID HANDLING --- */

RRComment.prototype.setId = (function () {
    var idCnt = 0;
    return function () {
        this.id = idCnt;
        idCnt++;
    };
}());

/* --- EXPORT --- */

exports.RRComment = RRComment;

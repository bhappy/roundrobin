/* --- SENDERS --- */

RRUser.prototype.sendUsername = function (username) {
    this.username = username;
};

RRUser.prototype.sendColor = function (color) {
    this.color = color;
};

RRUser.prototype.sendSocket = function (socket) {
    this.socket = socket;
};

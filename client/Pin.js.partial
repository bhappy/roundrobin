/* --- SENDERS --- */

Pin.prototype.sendUsername = function (username) {
    this.username = username;
};

Pin.prototype.sendColor = function (color) {
    this.color = color;
};

Pin.prototype.sendSocket = function (socket) {
    this.socket = socket;
};

Pin.prototype.sendComment = function(comment) {
    console.log('hi');
};
